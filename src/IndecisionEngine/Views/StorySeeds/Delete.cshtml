@model IndecisionEngine.Models.StorySeed

@{
    ViewData["Title"] = "Delete";
    var entries = (IEnumerable<StoryEntry>)ViewData["entries"];
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>StorySeed</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Title)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Title)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.StoryEntryId)
        </dt>
        <dd>
            @Html.DisplayFor(model => entries.FirstOrDefault(entry => entry.Id == model.StoryEntryId).Body)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            <a asp-action="Index">Back to List</a>
        </div>
    </form>
</div>
